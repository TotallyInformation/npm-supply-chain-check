# npm-supply-chain-check
A simple node.js CLI tool for checking for npm supply-chain risks. Give it an npm package name and optionally a starting folder (or "global") and it reports all uses of that package.
